services:
  - type: web
    plan: free
    name: justified
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "python -m daphne -b 0.0.0.0 -p $PORT server.wsgi:application"
  
    envVars:
    - key: DJANGO_SETTINGS_MODULE
      value: server.settings
    